{
  "name": "vitorpy.com",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "bundle install && bundle exec jekyll build"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0"
  }
}